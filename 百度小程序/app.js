/* globals App */
App({
    onLaunch(event) {
        console.log('onLaunch');
    },

    onShow(event) {
        console.log('onShow');
    },

    //kankan.

    globalData: {
        userInfo: 'user'
    }
});