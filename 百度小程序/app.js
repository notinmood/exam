/* globals App */
App({
    onLaunch(event) {
        console.log('onLaunch');
    },

    onShow(event) {
        console.log('onShow');
    },

    //kankan.

    //kaishi

    globalData: {
        userInfo: 'user'
    }
});